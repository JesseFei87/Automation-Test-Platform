# Task Plan (2026-06-03)

## Current Task

使用 `[@chrome](plugin://chrome@openai-bundled)` 直接打开百度搜索 `abc`，优先走 Codex Chrome 插件能力，不改动非目标文件。

## Steps

1. 读取 `doc/lessons.md` 并登记当前任务状态。
2. 按 Chrome 技能要求做轻量连接检查。
3. 若插件通道可用，则直接在 Chrome 打开百度搜索 `abc`。
4. 若插件通道不可用，则停在能力边界并明确记录故障层级。
