# Task Plan

## Current Task

实现首期 `Codex + Chrome` Web 自动化测试资产，交付可直接使用的目录结构、系统定义、用例模板、提示模板、报告模板和试点用例。

## Steps

1. 读取 `doc/` 过程文档并登记本次任务目标。
2. 基于当前空工作区设计首期测试资产目录与命名约定。
3. 落地试点系统定义、环境白名单说明与执行约束。
4. 落地 5 条 ICM 试点用例与对应提示模板。
5. 落地报告模板、截图目录与操作说明，并做结果校验。
## Current Task (2026-05-28 PM)

执行 `TC-ICM-007`，在已存在 `Test_Ins01` 的前提下，用 Chrome backend 进入 ICM 服务器信息页并完成服务器绑定设备验证。

## Steps (2026-05-28 PM)

1. 读取 `doc/lessons.md`、当前 case YAML 与 prompt 模板，确认前置条件与目标服务器信息需求。
2. 检查当前会话里的 Chrome backend 是否可接管，并定位可用标签页。
3. 在 Chrome 中执行 `TC-ICM-007`，保留 3 张关键截图并记录结果。

## Current Task (2026-05-28 PM 2)

执行 `TC-ICM-008`，在 `admin` 会话下进入用户管理并创建 `Tester` 用户。

## Steps (2026-05-28 PM 2)

1. 打开 `系统管理 > 用户管理` 并确认列表页可达。
2. 新建用户 `Tester`，填写部门、用户名、密码、岗位和角色。
3. 保存后确认列表中出现 `Tester`，并保留 3 张截图。
