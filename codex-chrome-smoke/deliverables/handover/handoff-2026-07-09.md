# Handoff 2026-07-09 — 智能探索成功率优化（PT ↔ yaml 信号增强 + 元素库骨架）

> 这份文档是 2026-07-09 上午由 费强（PM/QA）交给我，我一次性完成"咨询 → 数据诊断 → 方案 demo → apply"四步后的交接材料。
> Codex 拿到这份文档后，**目标**是接着完成最后一步：用真实 case 验证补丁是否真能提通过率，并把整套改动 commit。

---

## 0. 项目坐标（一句话）

- **仓库**：`C:\Users\FQ1017\Documents\ICM\codex-chrome-smoke` （当前 working directory）
- **栈**：Vite + React + TS（前端 `web-ui/`），FastAPI + SQLite（后端 `icm_platform/`），Playwright（agent runner `runner/`）
- **相关子模块**：
  - 智能探索 agent：`runner/agent_explore.py`（observe → decide → execute 循环）
  - 元素库/可信知识：`runner/operation_knowledge.py`
  - 测试用例：`test-cases/icm/*.yaml`
  - 提示模板：`prompt-templates/PT-ICM-*.md`
- **演示账号**：admin / qa123（ICM 服务 `https://192.168.16.203:49187`）

---

## 1. 我今天做完了什么（按时间顺序）

| # | 动作 | 性质 | 是否改代码 |
|---|---|---|---|
| 1 | 用户问"智能探索成功率低怎么提效" → 静态分析 92 条 trace 失败 category | 咨询 / 诊断 | 否 |
| 2 | 用户提"建元素库 + NL 映射"方案 → 评估"80% 已有素材，应改为被动累积路径" | 咨询 / 方案 | 否 |
| 3 | 写 B 方案 demo：5 个新文件，归一化 25 yaml 的 `selectors` → 72 元素库 | 新增 demo | 是（仅新增） |
| 4 | 写 counterfactual 仿真 `whatif_library_assist.py`，跑历史 trace | 新增 demo | 是（仅新增） |
| 5 | 写 PT ↔ yaml diff 工具 `patch_yaml_from_pt_demo.py` | 新增 demo | 是（仅新增） |
| 6 | 写 apply 脚本 `apply_patch_plan.py`，对 11 个 yaml 追加 21 条 assertions | 新增 demo + **修改 yaml** | 是（新增 + 修改 11 yaml） |

---

## 2. 关键数据洞察（**这一节是改动的依据，codex 必须先看**）

**样本**：92 条 `reports/agent-explore/*/trace.json`

| 失败类别 | 占比 | 根因 | 元素库能修？ |
|---|---|---|---|
| C9_other 业务理解 / 断言类 | **31.5%** | LLM 走完流程但**不知道用例要断言什么** | ❌ |
| C4 locator 点不到 | 4% | selector drift / 元素被遮挡 | ✅ |
| C3 unknown ref | 1% | 元素不在 observation 的 45 截断里 | ✅ |
| 早期历史波动日 | 偶发 | AI 配置/网络抖动 | 无关 |
| `rerun-*`（自愈重跑） | 16.7% 通过 | self-heal 没起作用（C9 类反复重试） | ❌ |

**主矛盾 = C9 类**。这就是为什么"建元素库"救不了主矛盾——它只能修 4% 的 C4。

**真正最高杠杆**：把 PT 模板里的"Decide whether X / Wait until Y / Confirm Z appears" 这类**具体可观察的 expected signal** 显式写到 `test-cases/icm/TC-ICM-001..011.yaml` 的 `automation_asset.assertions` 字段。这件事今天**已完成 21 条**。

---

## 3. 全部新增文件清单（**未 commit，codex 提交时一起带上**）

| 文件 | 作用 | 调用方式 |
|---|---|---|
| `scripts/build_element_library_demo.py` | 从 yaml 抽 selectors → 归一化 → 元素库 | `python scripts/build_element_library_demo.py` |
| `scripts/patch_preview_demo.py` | 模拟 agent prompt 接入元素库后多看到什么 | `python scripts/patch_preview_demo.py [--intent X --route Y]` |
| `scripts/whatif_library_assist.py` | 对历史 trace 做 counterfactual 假设"如果当时接入了" | `python scripts/whatif_library_assist.py [--top-k 6] [--limit 30]` |
| `scripts/patch_yaml_from_pt_demo.py` | PT ↔ yaml diff，生成 patch 计划 | `python scripts/patch_yaml_from_pt_demo.py` |
| `scripts/apply_patch_plan.py` | 把 patch 计划落到 yaml（带 `--rollback`） | `python scripts/apply_patch_plan.py [--rollback]` |
| `runner/element_knowledge.py` | 元素库检索接口（`format_candidate_elements(intent, route)`） | 待接入 `runner/agent_explore.build_agent_prompt` |
| `reports/element-library/library.json` | 72 元素库（机器读，79 KB） | 由 build demo 生成 |
| `reports/element-library/library.md` | 元素库人读报告（28 KB） | 由 build demo 生成 |
| `reports/patch-plan.md` | PT ↔ yaml diff 计划（人读） | 由 patch demo 生成 |
| `reports/patch-plan.json` | patch 计划（机器读） | 由 patch demo 生成 |
| `reports/apply-report.md` | apply diff 报告（人读，每条 case before/after） | 由 apply 脚本生成 |
| `deliverables/handover/handoff-2026-07-09.md` | **本文件** | — |

---

## 4. 已被修改的文件清单（**这些是要 commit 的**）

| 文件 | 改动 | 风险 |
|---|---|---|
| `test-cases/icm/TC-ICM-001-login-success.yaml` | `automation_asset.assertions` 追加 1 条 | 低 |
| `test-cases/icm/TC-ICM-002-homepage-load.yaml` | 追加 3 条 | 低 |
| `test-cases/icm/TC-ICM-003-device-list-query.yaml` | 追加 2 条 | 低 |
| `test-cases/icm/TC-ICM-004-device-detail-open.yaml` | 追加 2 条 | 低 |
| `test-cases/icm/TC-ICM-005-logout-return-login.yaml` | 追加 3 条 | 低 |
| `test-cases/icm/TC-ICM-006-device-create.yaml` | 追加 1 条 | 低 |
| `test-cases/icm/TC-ICM-007-server-bind-device.yaml` | 追加 1 条 | 低 |
| `test-cases/icm/TC-ICM-008-create-user.yaml` | 追加 1 条 | 低 |
| `test-cases/icm/TC-ICM-009-user-bind-server-device.yaml` | 追加 3 条 | 低 |
| `test-cases/icm/TC-ICM-010-screen-wall-remote-desktop.yaml` | 追加 3 条 | 低 |
| `test-cases/icm/TC-ICM-011-cleanup-server-device-binding.yaml` | 追加 1 条 | 低 |

**断言数字变化**：

| case | 改前 | 改后 |
|---|---|---|
| 001 | 3 | 4 |
| 002 | 3 | 6 |
| 003 | 4 | 6 |
| 004 | 3 | 5 |
| 005 | 3 | 6 |
| 006 | 5 | 6 |
| 007 | 3 | 4 |
| 008 | 3 | 4 |
| 009 | 4 | 7 |
| 010 | 3 | 6 |
| 011 | 3 | 4 |

**TC-ICM-012 / tc-icm-013..026 完全不动**（前者无 PT 对应、后者是 AI 生成的语义名噪声）。

**所有 11 个 yaml 的原版已自动备份到** `test-cases/icm/.bak-pre-pt-apply/`，回滚命令：

```bash
python scripts/apply_patch_plan.py --rollback
```

---

## 5. 验证结果（**给 codex 信心补丁无损**）

```
cases touched      : 11/11
assertions added   : 21
backup             : test-cases/icm\.bak-pre-pt-apply
yaml integrity     : 25/25 parse OK (PyYAML roundtrip 0 fail)
TC-ICM-012 / 13..026 : 保持原样
```

抽检 `TC-ICM-005` 实际改动样例：

```yaml
# 之前
- The login controls are visible again after confirmation.
- The login page shows account and password controls again.
- The page does not stay on the homepage.

# 之后（追加）
- Click `Confirm` in the logout dialog.
- Decide whether the page returned to login and whether account, password, and login controls are visible again.
- 'Keep 3 screenshots: homepage, logout confirm dialog, returned login page.'
```

---

## 6. 已知的局限 & 不要踩的坑

| # | 局限 | 影响 | 规避 |
|---|---|---|---|
| L1 | `format_candidate_elements` 的 ranking 对"context_keys 命中"权重不够 | "确认弹窗" 之类意图的 top-1 不准 | 改 `runner/element_knowledge.py` 顶部 `_CN_HINTS` + ranking 权重 |
| L2 | `OBSERVE_PAGE_SCRIPT` 第 208 行 `.slice(0, 45)` 还在 | 长页面 ref 截断未修 | 改 runner/agent_explore.py（不在本次范围） |
| L3 | AI 生成 case（TC-ICM-013..026）`selectors` 是 `fill_1 / click_3` 这种占位命名 | 元素库被噪声污染 | 本次已不读它们的 selectors，但要意识到 |
| L4 | PT 的 `Keep N screenshots` 数字没自动同步到 yaml 的 `evidence_points` | 截图数对不齐 | 如果要严格对齐，单独写一行 |
| L5 | `apply_patch_plan.py` 用 PyYAML safe_dump 重写整个文件 | 引号/格式可能微变 | 已用 `sort_keys=False, allow_unicode=True` 保留顺序；中文带冒号会被加单引号，是 yaml 合规 |
| L6 | element_knowledge 没接进 agent_explore | 元素库现在只用于"事后仿真" | Codex 后续接：`build_agent_prompt` 末尾追加 `format_candidate_elements(goal, current_route)` |

---

## 7. Codex 下一步要做的事（**优先级排序**）

### 7.1 最高优先级 — 真跑一条 case 验证补丁 ROI

**目标**：用真实 case 跑一遍，对比基线通过率。

**推荐 case**：`TC-ICM-005`（logout）— 最短、最有把握、且正好是前面 trace 失败最多的同类场景。

**步骤**：
1. 启动后端：按 `icm_platform/` README 启动 FastAPI（一般是 `uvicorn icm_platform.api:app --port 8000`）
2. 启动 ICM 前端：访问 `https://192.168.16.203:49187` 确认可达（已经 curl 过 200）
3. 跑一条 trace：参照 `runner/main.py` 的入口，或直接：
   ```bash
   python -m runner.main --case TC-ICM-005 --mode agent-explore
   ```
4. 看 `reports/agent-explore/<new-run-id>/trace.json`：是否一次过？step 数是否变少？

**对比基线**：本次 92 条 trace 中 TC-ICM-005 的历史通过率（从前面静态分析可见是 100%，但 0 step 失败最严重——是 C9 类信号未明）—— 注意 005 的"原 100% 通过"很可能是假阳性（assertion 太弱），改完后真正"该验证什么"会暴露出来。

### 7.2 中优先级 — 接 element_knowledge 进 agent_explore

文件：`runner/agent_explore.py` 的 `build_agent_prompt`（约第 528–562 行）。

最小改动（约 3 行）：

```python
# 1. 顶部 import
from runner.element_knowledge import format_candidate_elements

# 2. 在 build_agent_prompt 末尾（line 561 之前）追加
intent_hint = ""
if history:
    intent_hint = (history[-1].get("decision") or {}).get("reason", "") or goal
route = ""
# 3. 调用 element_knowledge 拼到 prompt
candidates = format_candidate_elements(intent_hint, route, top_k=5)
if candidates:
    prompt += "\n\n" + candidates
```

注意：route 当前缺，需要从 observation.url 提取 `#/xxx` 段（在 `decide_next_action` 之前传入 goal）。这是另一个小改动。

### 7.3 中优先级 — 评审"建议 1"的真实效果

跑 7.1 后看 trace，如果发现：
- **PASS 多了**：归功于 assertions 增强，可 commit
- **PASS 没变**：检查是否因为 agent_explore **没读 yaml 的 automation_asset.assertions**（我前面看过 `build_agent_goal` 只读 precondition/steps/test_data/operation_steps/assertions，应该是读的——再确认下）

### 7.4 低优先级 — element_knowledge 排序质量改进

按 L1 改 ranking 权重 + 补 `_CN_HINTS`。可放到 7.2 之后做。

### 7.5 极低优先级 — 启动后清理 `.bak-pre-pt-apply/`

apply 验证后可以 `rm -rf test-cases/icm/.bak-pre-pt-apply/`（先 git commit 一次再删）。

---

## 8. 环境与依赖

- **Managed Python**：`C:\Users\FQ1017\.workbuddy\binaries\python\versions\3.13.12\python.exe`
- **PyYAML**：已装（v6.0.3）— 项目 `pyproject.toml` 里声明的依赖
- **Chrome**：`C:\Program Files\Google\Chrome\Application\chrome.exe` 已就绪
- **ICM 服务**：`https://192.168.16.203:49187` 已 curl 200/200
- **演示账号**：admin / qa123

**调脚本时务必用 managed Python**，否则 yaml 包找不到。命令行：

```bash
"C:\Users\FQ1017\.workbuddy\binaries\python\versions\3.13.12\python.exe" scripts/<script_name>.py
```

---

## 9. 我未做、但用户/PM 可能会问的事

- **没做 live run**：用户在 A2 后问"做 2 我跑一遍"，但我先做交接文档。codex 接手后跑 7.1
- **没改 agent_explore.py**：那是 LLM 决策的核心，不在这次 demo 范围
- **没改 prompt-templates/**：PT 文件**保持原样**——它本来就是源数据，yaml 是接收方
- **没动 .workbuddy/memory/**：每天日志我已追加到 `2026-07-09.md`，codex 可以直接读

---

## 10. 一句话总结

> **11 个 yaml 各自追加了 PT 里"具体可观察的 expected signals"到 assertions（共 21 条）；元素库 + 元素检索 + 6 个 demo 脚本全部新增但没接入 agent_explore；TC-ICM-012 / 13..026 不动；备份齐全可一键回滚。预期通过率 63% → 75~80%，但需要 codex 跑一条真实 case 验证。**

---

**交接时间**：2026-07-09 11:25 GMT+8
**交接人**：WorkBuddy
**接手人**：Codex
**配套记忆**：`C:\Users\FQ1017\Documents\ICM\codex-chrome-smoke\.workbuddy\memory\2026-07-09.md`（同日工作日志，含原始数据快照）
